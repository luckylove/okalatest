<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.marinabay.cruise.dao.UserDao" >


  <resultMap id="userResultMap" type="com.marinabay.cruise.model.User" >
    <id column="page_id" property="pageId" jdbcType="BIGINT" />
    <result column="page_url" property="pageUrl" jdbcType="VARCHAR" />
    <result column="page_title" property="pageTitle" jdbcType="VARCHAR" />
  </resultMap>

  <sql id="Base_Column_List" >
    page_id, page_url, page_title
  </sql>
  
  <select id="select" resultMap="BaseResultMap" parameterType="map" >
    select
    <if test="distinct" >
      distinct
    </if>
    <include refid="Base_Column_List" />
    from page
    
    <if test="_parameter != null" >
      
    </if>
    
    <if test="orderByClause != null" >
      order by ${orderByClause}
    </if>
    
  </select>
  
  <select id="selectByID" resultMap="BaseResultMap" parameterType="java.lang.Long" >
    select 
    <include refid="Base_Column_List" />
    from page
    where page_id = #{pageId, jdbcType=BIGINT}
  </select>
  
  <delete id="deleteByID" parameterType="java.lang.Long" >
    delete from page
    where page_id = #{pageId,jdbcType=BIGINT}
  </delete>

 <!-- <insert id="insert" parameterType="com.namo.webanalytics.model.Page" >
    insert into page (page_url, page_title)
    values (
    	#{pageUrl,jdbcType=VARCHAR}, 
    	#{pageTitle,jdbcType=VARCHAR}
      )
      
    <selectKey resultType="java.lang.Long" keyProperty="pageId" >
         SELECT LAST_INSERT_ID() AS pageId
     </selectKey>

  </insert>
  
  
  
  <update id="update" parameterType="com.namo.webanalytics.model.Page" >
    update page
    set page_url = #{pageUrl,jdbcType=VARCHAR},
      page_title = #{pageTitle,jdbcType=VARCHAR}
    where page_id = #{pageId,jdbcType=BIGINT}
  </update>
  
  
  <select id="selectPage" resultMap="BaseResultMap" parameterType="java.lang.String" >
    select 
    <include refid="Base_Column_List" />
    from page
    where page_url = #{pageUrl, jdbcType=VARCHAR}
  </select>-->
  
</mapper>